<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">
    <context:annotation-config/>
    <context:component-scan base-package="com.mprojection"/>
    <context:property-placeholder location="classpath:db.properties"/>
    <bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator">
        <property name="proxyTargetClass" value="true"/>
    </bean>
    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig"
          p:poolName="springHikariCP"
          p:connectionTestQuery="SELECT 1"
          p:dataSourceClassName="com.impossibl.postgres.jdbc.PGDataSource">

        <property name="dataSourceProperties">
            <props>
                <prop key="host">${db.server}</prop>
                <prop key="port">${db.port}</prop>
                <prop key="database">${db.database}</prop>
                <prop key="user">${db.user}</prop>
                <prop key="password">${db.password}</prop>
                <prop key="preparedStatementCacheSize">${db.preparedStatementCacheSize}</prop>
                <prop key="parsedSqlCacheSize">${db.parsedSqlCacheSize}</prop>
            </props>
        </property>
    </bean>
    <!-- HikariCP configuration -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="shutdown"
          p:transactionIsolation="TRANSACTION_${db.transactionIsolation}">
        <constructor-arg ref="hikariConfig"/>
    </bean>
</beans>